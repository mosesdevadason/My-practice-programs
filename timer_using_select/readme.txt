This is a small program that implements a timer using select() API and a linked list data structure containing the required timeouts

Add calls to register_timeout() in the main function to add more timeouts

Compile using:

gcc timer.c -o timer
